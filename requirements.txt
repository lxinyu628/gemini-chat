# Web 框架和服务器
fastapi
uvicorn[standard]
gunicorn
python-multipart

# HTTP 客户端
requests[socks]
httpx[socks]

# 浏览器自动化（用于登录）
playwright

# 配置和环境变量
python-dotenv

# 文件监控（配置热重载）
watchdog

# 数据验证
pydantic

# CORS 支持
# fastapi 自带 starlette，包含 CORSMiddleware